[build-system]
requires = [
    "setuptools",
    "wheel",
    # we need at least 1.16.5 for a limited API bugfix
    "numpy==1.16.6; python_version<'3.8'",
    "oldest-supported-numpy; python_version>='3.8'"
]
build-backend = "setuptools.build_meta"
